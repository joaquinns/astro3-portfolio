---
import Card from '../components/Card.astro'
import Title from '../components/Title.astro'
import { data as projects } from '../data'
import Layout from '../layouts/Layout.astro'
---

<Layout title="Portfolio - Projects">
  <main
    class="max-w-screen-lg container mx-auto mt-16 px-4 lg:px-0 min-h-screen"
  >
    <Title>Projects</Title>
    <section
      class="grid grid-cols-templateAutoCols gap-4 justify-center items-center pt-4"
    >
      {
        projects.map((project) => (
          <Card
            id={project.id}
            imgUrl={project.imgUrl}
            title={project.title}
            body={project.body}
            href={`/project/${project.id}`}
          />
        ))
      }
    </section>
  </main>
</Layout>
